rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura para desarrollo
    // TODO: Cambiar estas reglas en producción
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
